#ifndef LESSON_FUNCTION_H
#define LESSON_FUNCTION_H
#include <map>

using std::string;

bool need_reset(std::map<string, string>&, const string&, const string&);

int Random (int, int);

long int sum (int);

bool check_flag(int, char**);

bool check_date(const string&, const string&, const string&);

bool check_sex(const string&);

#endif //LESSON_FUNCTION_H